global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'your-app'
    static_configs:
      - targets: ['127.0.0.1:8474']  # Adjust port as needed
    metrics_path: '/metrics'
    scrape_interval: 5s
